import 'package:flutter/material.dart';

class Pallet {
  static const primaryColor = Color.fromRGBO(245, 245, 245, 1); // primary color
  static const secondaryColor = Color.fromRGBO(208, 121, 63, 1); // secondary color
  static const darkColor = Color.fromRGBO(18, 20, 22, 1); // text color
  static const greyColor = Color.fromRGBO(224, 224, 224, 1);
  static const whiteColor = Colors.white;
  static var redColor = Colors.red.shade500;
  static var blueColor = Colors.blue.shade300;
  static const Color searchBarColor = Color.fromRGBO(32, 35, 39, 1);
}
